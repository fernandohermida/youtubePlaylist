{
  "name": "youtube-live-playlist-sync",
  "version": "1.0.0",
  "description": "Automatically sync YouTube playlists with currently live streams from configured channels",
  "main": "dist/api/cron.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["youtube", "playlist", "automation", "vercel", "cron"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0"
  }
}
